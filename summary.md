# 工作摘要

**版本:** 1.2.0
**时间:** 2026-05-11

## v1.2.0 变更概要

移除 external/tdxdata 依赖，改为本地仓库 tdxdata editable 安装；优化 UI 分页与数据展示；简化导航流程。

### 依赖调整

- 移除 `external/tdxdata` 概念引用，tdxdata 改为通过本地仓库 `~/peiking88/tdxdata` 以 editable 模式安装
- `pyproject.toml` 正式声明 tdxdata 依赖，移除 external 排除规则
- 更新 `CLAUDE.md`、`README.md`、`tdxdata_source.py` 中所有 external 相关描述

### UI 优化

- 分页表格按日期倒序排列（近日在前），修复页码越界问题
- 价位/金额列统一格式化为 `%.2f`
- 日期选择器默认结束日期改为当天
- 导航简化：移除认证流程，默认进入图表分析页

### 数据层改进

- 数据源添加去重逻辑（同名同类型返回已有 ID）
- 仪表板创建添加重名检查
- 数据库 init 脚本：data_sources 表增加 UNIQUE 约束，指标/仪表板防重复插入
- 健康检查结果持久化到数据库

### 目录结构

- 新增 `cfg/`（配置文件目录）和 `output/`（输出文件目录）

### 测试

- 单元测试：355 passed，覆盖率 87.24%

---

## v1.1.0 全类型数据导入/缓存/增量/重导闭环

**时间:** 2026-05-10

---

## v1.0.0 初始版本

**时间:** 2026-05-10
