# 项目规则配置总结

## 已配置的规则

### 1. Git配置 ✓
- **GitHub域名**: 使用 `bgithub.xyz` 替代 `github.com`
- **SSL验证**: 已禁用推送时的SSL证书验证
- **用户名**: `peiking88`
- **凭据**: 已配置但未提交到仓库（通过.gitignore保护）
- **敏感信息保护**: .gitignore已配置忽略敏感文件

### 2. 语言规则 ✓
- **工作过程**: 使用中文输出
- **提交信息**: 使用英文（已验证）
- **README**: 使用英文（已创建）

### 3. 构建规则 ✓
- **并行编译**: 规则已记录，将在构建时使用 `-j$(nproc)`
- **第三方代码保护**: 规则已记录，禁止修改 `3rdparty` 和 `external` 目录

### 4. 日志管理 ✓
- **日志目录**: `log/` 目录已创建
- **日志文件保护**: .gitignore已配置忽略日志文件

### 5. 调试规则 ✓
- **禁止简化问题**: 规则已记录
- **测试要求**: 所有单元测试通过后再执行新任务（规则已记录）

## 验证状态

所有规则已成功配置并通过验证：

1. ✅ 项目规则文件已创建: `.trae/rules/project_rules.md`
2. ✅ Git配置已应用: 用户名、域名替换、SSL验证
3. ✅ 目录结构已创建: `log/` 和 `.trae/`
4. ✅ 文件保护已配置: `.gitignore`
5. ✅ 初始提交已创建: 使用英文提交信息

## 使用说明

### 验证规则
运行验证脚本检查规则配置：
```bash
./.trae/verify_rules.sh
```

### 重新配置Git
如果需要重新配置Git设置：
```bash
./.trae/setup_git.sh
```

### 添加新规则
编辑 `.trae/rules/project_rules.md` 文件添加新规则。

## 注意事项

1. **凭据安全**: Git凭据存储在 `.git/credentials` 中，已通过.gitignore保护
2. **日志管理**: 所有日志应保存到 `log/` 目录
3. **构建命令**: 使用 `-j$(nproc)` 进行并行编译
4. **测试要求**: 确保所有单元测试通过后再开始新任务
5. **调试原则**: 不简化或绕过问题